// NOTE: Currently no one is assigned to this

// Transfer anon user to a different agent

// Set user to online/offline (when socket opens/closes)
    // If agent goes offline, it should transfer all of their users giving them priority
    // (via adding them to front of help_desk_queue)
    // Should also give the new agent their transcript